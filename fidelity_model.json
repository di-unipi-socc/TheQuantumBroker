{
    "weights": {
    "fidelity_weight": 1.0
    },
    "constraints": [
        "sum(shots*cost) <= 13000",
        "min(shots) >= 200",
        "max(execution_time + waiting_time) <= 10000",
        "sum(used) >=3"
    ],
  "objective": "max(fidelity)"
}