{
    "weights": {
      "time_weight": 0.5,
      "cost_weight": 0.5
    },
    "constraints": [
        "sum(cost) <= 15000",
        "min(shots) >= 100",
        "min(fidelity) >= 0.9",
        "max(execution_time + waiting_time) <= 80000"
    ],
  "objective": "min(fidelity)"
}