{
  "total_shots": 1024,
  "weights": {
    "cost_weight": 1.0
  },
  "constraints": [
    "sum(shots*cost) <= 750",
    "shots['fake_torino'] >= 50",
    "min(fidelity) >= 0.89",
    "min(shots) >= 30"
  ],
  "objective": "sum(shots*cost)"
}