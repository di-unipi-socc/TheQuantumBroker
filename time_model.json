{
    "weights": {
      "time_weight": 0.5,
      "cost_weight": 0.5
    },
    "constraints": [
        "sum(cost) <= 450",
        "min(shots) >= 50",
        "min(fidelity) >= 0.19",
        "max(execution_time + waiting_time) <= 1500"
    ],
  "objective": "time_weight * ((execution_time + waiting_time))+ cost_weight*(cost)"
}