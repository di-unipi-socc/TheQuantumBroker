{
  "total_shots": 1024,
  "weights": {
    "time_weight": 1.0
  },
  "constraints": [
    "sum(cost) <= 3600",
    "max(shots * execution_time + used * waiting_time) <= 1300",
    "min(fidelity) >= 0.9"
  ],
  "objective": "max(shots * execution_time + used * waiting_time)"
}