{
  "total_shots": 1024,
  "weights": {
    "time_weight": 1.0
  },
  "constraints": [
    "sum(cost*shots) <= 3500",
    "min(fidelity) >= 0.85",
    "min('shots') >= 1"

  ],
  "objective": "max(execution_time + waiting_time)"
}